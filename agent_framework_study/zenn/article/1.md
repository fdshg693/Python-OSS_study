
```python
response = await client.get_response(message, tools=add_nums)
```

get_responseのtoolsは以下のように、ToolProtocol・Callable・MutableMappingの3種類そして、それらのSequenceを受け取れます
今回はadd_numsはシンプルな関数なので、Callable[..., Any]に当たります。

```python
tools: ToolProtocol
    | Callable[..., Any]
    | MutableMapping[str, Any]
    | Sequence[ToolProtocol | Callable[..., Any] | MutableMapping[str, Any]]
    | None = None,
```

最初に以下でtoolsの正規化を行います。
とはいえ、ここでは単純にlist型のlist[ToolProtocol | dict[str, Any] | Callable[..., Any]]に変換しているだけです。

```python
normalized_tools = await self._normalize_tools(tools)
```